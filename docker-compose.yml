services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:80"

  game-two-ships:
    build:
      context: ./games/two_ships
    ports:
      - "9090:80"

  game-space-word:
    build:
      context: ./games/space_word
    ports:
      - "8081:80"